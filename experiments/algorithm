INITIALIZE arr[] = {1, 2, 3, 4, 5}
SET h = a[0] 
SET l = a[0]

SET k = 4

WHILE k > 0
	if a[k] > h, then,
		h = a[k]
	if a[k] < l, then,
		l = a[k]
	k -= 1

PRINT "smallest int: ", l
PRINT "largest int: ", h
